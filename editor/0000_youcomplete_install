
1. 更新python

1.1 pyenv

在.bashrc添加
export PYENV_ROOT="$HOME/.pyenv"
export PATH="$PYENV_ROOT/bin:$PATH"
eval "$(pyenv init -)"

安装3.8.0
pyenv install 3.8.0
切换3.8.0
pyenv global 3.8.0

如果不想使用pyenv管理python，则把bashrc中的语句去掉，就可以了。


1. 更新glibc
https://blog.csdn.net/xihuanyuye/article/details/126865893
cd glibc-2.29
mkdir build
cd build
../configure --prefix=/usr/local/glibc

ll /usr/local/glibc/lib/libm-2.29.so
cd /lib/x86_64-linux-gnu
ln -s /usr/local/glibc/lib/libm-2.29.so libm.so.6


2. 更新libstdc++6
sudo add-apt-repository ppa:ubuntu-toolchain-r/test
sudo apt update
sudo apt install libstdc++6

3. 更新cmake




